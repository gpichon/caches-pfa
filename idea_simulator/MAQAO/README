Pour lancer le script de création des traces par coeur, faire:

./tests.sh test.c par 4

avec:
- test.c le nom du fichier
- par la fonction à instrumenter
- 4 le nombre de threads


Filter sur numero instruction:
decommenter suppressions de fichiers dans tests.sh
./tests.sh test.c par 4 | awk ' ~ /15/' | awk '{print }'
