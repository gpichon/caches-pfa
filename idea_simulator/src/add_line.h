#ifndef ADD_LINE_H
#define ADD_LINE_H

#include <stdio.h>
#include <stdlib.h>

/*
  Module utilisé pour ajouter une ligne de cache dans un cache.
  Gère la politique de remplacement.
*/

/*
  search_line_to_replace()
  init_new_line(flags)
  assert_coherency()
*/


#endif
