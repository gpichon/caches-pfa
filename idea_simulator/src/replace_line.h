#include <stdio.h>
#include <stdlib.h>

/*
  Module utilisé pour chercher une ligne qui n'est pas dans le cache
  actuel.  Actuellement, cherche dans le cache au dessus et recommence
  si nécessaire.  Retourne le nombre de caches parcouru avant de
  trouver (ou non) la valeur recherchée.
*/

/*
  get_higher_cache()
  search_into()
*/
